{
  "name": "Portfólio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch-scss": "sass --watch css/style.scss css/style.css --style compressed",
    "dev-all": "npm run dev & npm run watch-scss",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
